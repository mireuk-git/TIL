# CodaKata
### 30189
[link](https://www.acmicpc.net/problem/30189)
#### python
```python
n,m = map(int,input().split())
r=0
f=1
for s in range(min(n,m)+1):
    r+=f
    f+=1
f-=1
for s in range(min(n,m)+1,max(n,m)+1):
    r+=f
f-=1
for s in range(max(n,m)+1,n+m+1):
    r+=f
    f-=1
print(r)
```
- 수식에서 변수 하나가 정해지면 다른 하나의 변수는 값이 고정
- 크게 경우의 수가 세가지로 나뉨
- `0<=i<=min(n,m)`일 경우 가짓수가 1부터 1씩 늘어남
- `min(n,m)+1<=i<=max(n,m)`일 경우 가짓수가 `min(n,m)`으로 고정
- `max(n,m)<=i<=n+m`일 경우 가짓수가 1씩 줄어 최종적으로 1이 됨

#### java
```java

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class p30189 {
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken());
        int m = Integer.parseInt(st.nextToken());
        int small, big;
        if (n<m) { small = n; big = m; }
        else { small = m; big = n; }

        int cnt = 0;
        for (int i=0;i<=small;i++){ cnt+=i+1; }
        for (int i=small+1;i<=big;i++){ cnt+=small+1; }
        for (int i=big+1;i<=big+small;i++){ cnt+=small+1-i+big; }
        System.out.println(cnt);
    }
}
```


# 정보처리기사
## 국제표준
### 제품 품질
제품에 대한 품질 확인

#### ISO/IEC 9126
|NAME|MEANING|
|---|---|
|기능성|`요구사항`에 만족하는 기능에 대한 제품의 능력|
|신뢰성|성능 수준을 `유지`할 수 있는 제품의 능력|
|사용성|이해하고, 학습하고, 사용하기 `편리`한 정도|
|효율성|`효율적`으로 `자원`을 사용하는 제품의 능력|
|유지보수성|요구사항을 반영해 `제품을 변형`하기 용이한 능력|
|이식성|`현재 환경에서 다른 환경으로` 이전되기 용이한 능력|

#### ISO/IEC 14598
품질 특성을 평가하는 과정의 특성(평가의 평가)
|반복성|`동일 평가자`, 동일 사양, `동일한 결과`|
|재현성|`다른 평가자`, 동일 사양, `비슷한 결과`|
|공정성|평가가 특정 결과에 편향되지 않아야 함|
|객관성|평가자의 `감정이나 의견`에 영향을 받지 않아야 함|

#### ISO/IEC 12119
테스트 종류
|제품설명서|소프트웨어 패키지의 속성을 설명|
|사용자문서|사용 가능한 전체 문서들의 집합|
|실행프로그램|요구사항이 명확하게 정의된 대상|

#### ISO/IEC 25000
ISO/IEC 9126+14598
|ISO/IEC 2500n|품질 `관리` 부분|
|ISO/IEC 2501n|품질 `모델` 부분|
|ISO/IEC 2502n|품질 `측정` 부분|
|ISO/IEC 2503n|품질 `요구사항` 부분|
|ISO/IEC 2504n|품질 `평가` 부분|

### 프로세스
#### ISO/IEC 12207
|**기본** 생명주기 프로세스|획득,공급,개발,운영,유지보수|
|**지원** 생명주기 프로세스|문서화,형상관리,품질보증,검증,확인,합동검토,감사,문제해결|
|**조직** 생명주기 프로세스|관리,기반구조,개선,교육훈련|
기본 생명주기 - 기본적인 개발 프로세스
지원 생명주기 - 기본 생명주기를 지원하는 프로세스
조직 생명주기 - 조직이 지원해주는 프로세스

#### ISO/IEC 15504(SPICE)
|0|불안정단계(Incomplete)|미구현, `목표 미달성`|
|1|수행단계(Performed)|프로세스 수행 및 목적 달성|
|2|관리단계(Managed)|프로세스 수행 계획 및 관리|
|3|확립단계(Established)|`표준 프로세스`의 사용|
|4|예측단계(Predictable)|프로세스의 `정량적 이해` 및 통제|
|5|최적화 단계(Optimizing)|프로세스의 `지속적`인 개선|

#### CMM
|1|초기단계|`관리X`|
|2|반복단계|`프로세스의 반복`|
|3|정의단계|프로젝트 관리(`표준 프로세스`)|
|4|관리단계|`정량적 관리`|
|5|최적화단계|`지속적`인 개선|

#### CMMi
|1|초기단계|프로세스X|
|2|관리단계|기본적 프로세스|
|3|정의단계|`표준 프로세스`|
|4|정량적 관리 단계|`정량적 관리`|
|5|최적화 단계|`지속적`개선|
