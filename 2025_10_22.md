# Codekata
### 5566
[link](https://www.acmicpc.net/problem/5566)
#### python
```python
n,m=map(int,input().split())
x=[]
for _ in range(n): x.append(int(input()))
st=0
cnt=0
d=[]
for _ in range(m): d.append(int(input()))
for j in d:
    cnt+=1
    st+=j
    if st<=n-1: st+=x[st]
    if st>=n-1: break
print(cnt)
```

#### java
```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class p5566 {
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken()), m = Integer.parseInt(st.nextToken());
        int[] x = new int[n];
        for (int i=0;i<n;i++) {
            st = new StringTokenizer(br.readLine());
            x[i] = Integer.parseInt(st.nextToken());
        }
        int cnt = 0, z = 0;
        int[] dice = new int[m];
        for (int  j=0; j<m; j++){
            st = new StringTokenizer(br.readLine());
            dice[j] = Integer.parseInt(st.nextToken());
        }

        for (int j=0; j<m; j++){
            cnt++;
            z+=dice[j];
            if (z<n) z+=x[z];
            if (z<0) z=0;
            if (z>=n-1) break;
        }
        System.out.println(cnt);
    }
}
```
- python 풀이에서 실수로 빼먹은게 있는데 말이 있는 칸을 다루는 변수인 `z`(`st`)는 음수가 될 수 없다. 따라서 0보다 낮아지면 0으로 만드는 코드를 삽입해야 한다.

# 정보처리기사
## 서브넷
#### IP
IP주소: 도착지와 출발지를 표기하는 주소, 네트워크 표기 + 호스트 표기, 마지막 주소는 브로드캐스트

A클래스: 첫 옥텟이 0~127(0xxx.), 첫 옥텟 만으로 네트워크 식별
B클래스: 첫 옥텟이 128~191(10xx.), 두 옥텟으로 네트워크 식별
C클래스: 첫 옥텟이 192~223(110x.), 세 옥텟으로 네트워크 식별
D클래스, E클래스: 멀티태스킹, 연구용으로 사용

한계: 자원의 낭비
하나의 네트워크에서 사용할 수 있는 주소 수보다 호스트 수가 적으면 자원의 낭비

#### 서브넷 마스크
IP주소 영역을 적절하게 나누는 기술
IP 주소와 서브넷 마스크를 AND연산하면 IP주소의 네트워크 ID
서브넷 마스크에서 1로 표현된 비트들은 네트워크 식별영역
0으로 표현된 비트들은 호스트 식별 영역
CIDR 표기법에서는 `X.X.X.X/X`식의 IP주소에서 `/X`식으로 서브넷 마스크에서 1비트의 개수를 표기함
IP subnet-zero가 적용되지 않으면 subnet 개수에서도 호스트 주소와 같이 네트워크 주소와 브로드캐스트 주소가 빠져야함(-2)
아무말 없으면 IP subnet-zero가 적용되지 않는걸로 취급

> 01\. IPv4 주소 **A**클래스에 대한 **표준** 네트워크 서브넷 마스크로 옳은 것은? 
>1. 255.0.0.0        V
>2. 255.255.0.0
>3. 255.255.255.0
>4. 255.255.255.255

A클래스는 첫 옥텟을 네트워크 식별영역으로 사용함
B클래스 - 255.255.0.0
C클래스 - 255.255.255.0

> 02\. 다음 중 IP주소가 **B** class이고, **전체를 하나의 네트워크망으로 사용**하고자 할 때 적절한 서브넷 마스크 값은?
>1. 255.0.0.0
>2. 255.255.0.0 V
>3. 255.255.255.0
>4. 255.255.255.255

전체를 하나의 네트워크망으로 사용 - 서브네팅을 하지 않겠다, 표준 네트워크 서브넷 가져와라

> 03\. IP address 체계의 **C** class의 **기본** 서브넷 마스크에 해당하는 것은?
>1. 255.0.0.0
>2. 255.255.0.0
>3. 255.255.255.0 V
>4. 255.255.255.255

> 04\. 다음 중 192.168.0.1/25가 의미하는 서브넷 마스크 값으로 알맞은 것은?
>1. 255.255.0.0
>2. 255.255.255.0
>3. 255.255.255.128 V
>4. 255.255.255.192

IP는 C클래스
서브넷 마스크의 1비트 수는 25개(1111 1111.1111 1111.1111 1111.1000 0000) -> 255.255.255.128

> 05\. 아래 내용에 해당하는 서브넷 마스크 값은? 203.76.25.1/26
>1. 255.255.255.192 V
>2. 255.255.255.224
>3. 255.255.255.254
>4. 255.255.255.0

1111 1111.1111 1111.1111 1111.1100 0000
128+64=192

> 06\. IPv4의 **C** 클래스 네트워크를 **26개의 서브넷**으로 나누고, 각 서브넷에는 **4~5개의 호스트**를 연결하려고 한다. 이러한 서브넷을 구성하기 위한 서브넷 마스크 값은? 
>1. 255.255.255.192
>2. 255.255.255.221
>3. 255.255.255.240
>4. 255.255.255.248 V

2^5=32, 5비트를 할애하면 최대 32개의 서브넷으로 나눌 수 있음
255.255.255.X/24+5
128+64+32+16+8=248
255.255.255.248

> 07\. 클래스**B** 주소를 가지고 서브넷 마스크 255.255.255.240으로 서브넷을 만들었을 떄 나오는 서브넷의 수와 호스트의 수가 맞게 짝지어진 것은? 
>1. 서브넷 2048, 호스트 14
>2. 서브넷 14, 호스트 2048
>3. 서브넷 4094, 호스트 14  V
>4. 서브넷 14, 호스트 4094
240=128+64+32+16
2^12-2=4094, 2^4-2=14

> 08\. **B** class 네트워크에서 6개의 서브넷이 필요할 때, 가장 많은 호스트를 사용할 수 있는 서브넷 마스크 값은?
>1. 255.255.192.0
>2. 255.255.224.0   V
>3. 255.255.240.0
>4. 255.255.248.0

255.255.128+64+32.0

#### 서브넷 계산

> 01\. 192.168.1.0 **/24** 네트워크를 FLSM 방식으로 **4개**의 Subnet으로 나누고 **IP Subnet-zero**를 적용했다. 이때 Subnetting된 네트워크 중 **4번째 네트워크의 4번째 사용 가능한 IP**는 무엇인가?
>1. 192.168.1.192
>2. 192.168.1.195
>3. 192.168.1.196   V
>4. 192.168.1.198

타입: C타입(192., `110x.`)
서브넷 마스크: 255.255.255.128+64
(첫번째, 마지막 사용불가) -> IP Subnet-zero
4번째 네트워크: 192.168.1.192
(첫번째와 마지막은 사용불가)
4번째 IP: 192.168.1.192+4=196

