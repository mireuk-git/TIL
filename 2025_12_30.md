# Codekata
### 17252
[link](https://www.acmicpc.net/problem/17252)
#### python
```python
n = int(input())
s=n
for i in range(19,-1,-1):
    if 3**i<=s:
        s-=3**i
if n!=0 and s==0: print("YES")
else: print("NO")
```
- 3의 거듭제곱 3개로 만드는 수로 착각해서 시간복잡도 O(n^3)짜리의 프로그램을 먼저 만들었는데, 항이 3개에 국한되지 않는다는 걸 깨닫고 그리디 알고리즘으로 프로그램을 수정했다. 결과적으로 O(n)의 더 효과적인 프로그램이 나왔다.
- 3의 거듭제곱인 수가 항에 반드시 하나는 포함되어야 하기 때문에 입력값 `0`인 경우에는 `NO`가 출력되어야 한다.

#### java
```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class p17252 {
    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        int n = Integer.parseInt(st.nextToken());
        int s = n;
        for (int i=19;i>=0;i--){
            if (Math.pow(3,i)<=s){
                s-=Math.pow(3,i);
            }
        }
        if (n!=0 && s==0) System.out.println("YES");
        else System.out.println("NO");
    }
}
```

# 정보처리기사
### 퍼미션
특정 파일이나 경로를 사용할 권한을 설정하는 기준, 그 설정 그 자체
유닉스/리눅스에선 3자리의 8진수로 권한설정

파일의 소유자 본인(U) | 소유자와 같은 그룹의 사용자(G) | 그외 일반 사용자(O)
읽기(r),쓰기(w),동작(x) | 읽기,쓰기,동작 | 읽기,쓰기,동작
421 | 421 | 421

chmod - 파일의 권한, 모드 변경
- `chmod [1][2][3] [파일]`
|[1]|파일 소유자(u),그룹(g),그 외 사용자(o), 모든 사용자(a)|
|[2]|추가(+),제거(-),지정(=)|
|[3]|읽기(r),쓰기(w),실행(x)|
- `chmod [3자리 8진수] [파일]`

chown - 소유자 변경
- `chown [옵션] [소유자]:[소유그룹] [파일 또는 디렉토리]`

> 01\. `test.txt` 파일이 다음과 같은 권한을 가지기 위해 작성할 `명령`을 쓰시오.
> -rwxrw-r-x

421 42- 4-1
`chmod 765 test.txt`

> 02\. `test.txt`에 대해 `사용자`는 `읽기, 쓰기, 실행 권한`을 부여하고, `그룹`과 `다른 사용자`는 `읽기와 실행 권한`만을 지정하는 명령을 쓰시오. 

rwx r-x r-x
755
`chmod 755 test.txt`

> 03\. `test.txt` 파일이 다음과 같은 권한을 가지기 위해 실행할 명령을 쓰시오. 
> -rwxr-xr--

421 4-1 4--
754
`chmod 754 test.txt`