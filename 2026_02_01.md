# 정보처리기사
## 프로그래밍 언어 활용

#### C
>01\. C언어에서 정수 자료형으로 옳은 것은?
>1. int V
>2. float
>3. char 
>4. double 

long도 있다.

#### C

>02\. 다음 C프로그램의 결과 값은?
 ```C
main(void){
    int i;
    int sum=0;
    for (i=1;i<=10;i=i+2)
        sum = sum+i;
    printf("%d",sum);
}
```
>1. 15
>2. 19
>3. 25 V
>4. 27

1+3+5+7+9 = 25

#### UNIX
>03\. UNIX에서 새로운 프로세스를 생성하는 명령어는?
>1. ls
>2. cat X
>3. fork V
>4. chmod

ls - 디렉토리의 내용물 출력
cat - 문자열 합성
chmod - 권한 변경

#### OSI-7계층
>04\. OSI-7 Layer에서 링크의 설정과 유지 및 종료를 담당하며, `노드 간`의 오류제어와 흐름제어 기능을 수행하는 계층은?
>1. 데이터링크 계층 V
>2. 물리 계층
>3. 세션 계층
>4. 응용 계층

OSI-7계층
==
응용(http, telnet, dhcp, dns, ftp, ssh, smtp, snmp)
표현(mime,tls,ssl,jpeg,mpeg,smb,afp)
세션(ssh,tls,rpc)
==
전송(tcp,udp,sctp,rtp)
네트워크(라우터)(ip,arp,icmp,igmp,rip,rarp)
==
데이터링크(스위치, 브리지)(ethernet,atm,ppp), 노드간 연결
물리(허브, 리피터)

#### 결합도
>05\. 다음 중 결합도가 가장 강한 것은?
>1. data coupling 
>2. stamp coupling
>3. common coupling V
>4. control coupling

|결합도|내용|
|---|---|
|데이터(`data`)|자료값만 전달|
|스탬프(`stamp`)|배열,오브젝트,스트럭쳐 전달|
|제어(`control`)|전달되는게 다른 모듈의 제어 요소|
|외부(``)|외부로 선언한 변수를 다른 모듈에서 참조|
|공통(`common`)|전역변수 참조|
|내용(`content`)|다른 모듈 내부의 변수나 기능을 다른 모듈에서 사용|

#### Subnet
>06\. `200.1.1.0/24` 네트워크를 FLSM 방식을 이용하여 `10개`의 subnet으로 나누고 ip subnet-zero를 적용했다. 이때 서브네팅된 네트워크 중 `10번째 네트워크`의 `broadcast IP` 주소는?
>1. 200.1.1.159 V
>2. 201.1.5.175
>3. 202.1.11.254
>4. 203.1.255.245

128+64+32+16
서브넷 마스크: 255.255.255.240
11번째 네트워크: 200.1.1.160
10번째 네트워크의 broadcast IP: 200.1.1.159

#### 취약점
> 07\. 다음 내용이 설명하는 소프트웨어 취약점은?
> `메모리`를 다루는 데 오류가 발생하여 잘못된 동작을 하는 프로그램 취약점
>1. FTP 바운스 공격
>2. SQL 삽입
>3. 버퍼 오버플로 V
>4. 디렉토리 접근 공격 X

소프트웨어 보안 취약점
- 메모리
  - 버퍼 오버플로: 메모리 오류가 발생해 잘못된 동작
  - 허상 포인터: 유효한 객체를 가리키고 있지 않은 포인터로 인한 취약점
  - NULL POINTER 역참조: NULL로 설정된 변수의 주소값 참조시 발생하는 취약점
- 입력확인 오류
  - 포맷 스트링 버그: 검사되지 않은(상정되지 않은) 사용자 입력
  - SQL 삽입: SQL문이 실행되도록 입력값 설정(쿼리 삽입)
  - 코드 삽입: 유효하지 않은 데이터 실행(스크립트 코드)
  - 웹 사이트간 스크립팅(XSS): 부정한 HTML이나 javascript 삽입
  - 이메일 삽입
  - 디렉토리 접근 공격
  - HTTP 헤더 인젝션
  - HTTP 응답 스플리팅
- 경쟁상태
  - Time of check to time of use: 동기화 오류
  - 심볼릭 링크 경쟁: 심볼릭 링크를 이용한 공격
- 권한 혼동 버그
  - 웹에서 사이트간 요청위조
  - 클릭 재킹
  - FTP 바운스 공격

#### TCP 프로토콜
>08\. TCP 프로토콜에 대한 설명으로 거리가 먼 것은?
>1. 신뢰성 있는 연결 지향형 전달 서비스이다. X
>2. 기본 헤더 크기는 100byte이고 160byte까지 확장 가능하다.
>3. 스트림 전송 기능을 제공한다.
>4. 순서제어, 오류제어, 흐름제어 기능을 제공한다. 

기본 헤더 크기는 20byte

#### python
>09\. 파이썬의 변수 작성 규칙 설명으로 옳지 않은 것은?
>1. 첫 자리에 숫자를 사용할 수 없다.
>2. 영문 대문자/소문자, 숫자, 밑줄(_)의 사용이 가능하다. 
>3. 변수 이름의 중간에 공백을 사용할 수 있다.V
>4. 이미 사용되고 있는 예약어는 사용할 수 없다. 

#### 배치 프로그램 필수 요소
>10\. 배치 프로그램의 필수 요소에 대한 설명으로 틀린 것은?
>1. 자동화는 심각한 오류 상황 외에는 사용자의 개입 없이 동작해야 한다.
>2. 안정성은 어떤 문제가 생겼는지, 언제 발생했는지 등을 추적할 수 있어야 한다. X
>3. 대용량 데이터는 대용량의 데이터를 처리할 수 있어야 한다. X
>4. 무결성은 주어진 시간 내에 처리를 완료할 수 있어야 하고, 동시에 동작하고 있는 다른 애플리케이션을 방해하지 말아야 한다. V

배치 프로그램의 필수 요소
|요소|설명|
|---|---|
|대용량 데이터|대용량의 데이터 처리|
|자동화|사용자의 개입없이 동작|
|견고함|비정상적인 동작 중단 발생X|
|안정성|어떤 문제가 생겼는지, 언제 발생했는지 추적|
|성능|주어진 시간 내에 처리, 동시에 동작하는 다른 애플리케이션 방해X|

#### 응집도
>11\. 다음이 설명하는 응집도의 유형은?
> 모듈이 다수의 관련 기능을 가질 때 모듈안의 구성 요소들이 그 기능을 순차적으로 수행할 경우의 응집도
>1. 기능적 응집도
>2. 우연적 응집도
>3. 논리적 응집도
>4. 절차적 응집도 V

|응집도|내용|
|---|---|
|기능적|모든 기능이 단일 목적 수행|
|순차적|한 활동으로부터 나온 출력값을 다른 활동에서 사용|
|통신적|동일한 입력과 출력을 사용해 다른 기능 수행|
|절차적|모듈 안 구성요소들이 기능을 순차적으로 수행|
|시간적|특정 시간에 처리되어야 하는 행동 모집|
|논리적|유사한 성격을 한 모듈에서 처리|
|우연적|연관없음|

#### IPv6
>12\. IPv6에 대한 설명으로 틀린 것은?
>1. 32비트의 주소체계를 사용한다. V
>2. 멀티미디어 실시간 처리가 가능하다.
>3. IPv4보다 보안성이 강화되었다.
>4. 자동으로 네트워크 환경구성이 가능하다. 

|구분|IPv4|IPv6|
|주소길이|32bit|128bit|
|표시방법|8bit씩 4부분|16bit씩 8부분|
|주소개수|약 43억|약 31조|
|QoS|Best Effort|등급별, 서비스별 패킷 구분|
|보안기능|IPSec|확장기능에서 기본제공|
|plug&play|X|O|
|모바일IP|X|O|
|웹캐스팅|X|O|
|전송방식|BroadCast|AnyCast|

IPv6
네트워크 규모 및 단말기 수에 따라 순차할당
품질보장, 헤더크기 가변

#### java
>13\. 다음 자바 프로그램 조건문에 대해 삼항 조건 연산자를 사용하여 옳게 나타낸 것은?
```java
int i=7, j=9;
int k;
if (i>j)
    k=i-j;
else 
    k=i++j;
```
>1. int i=7,j=9; int k; k=(i>j)?(i-j):(i+j) V
>2. int i=7,j=9; int k; k=(i<j)?(i-j):(i+j)
>3. int i=7,j=9; int k; k=(i>j)?(i+j):(i-j)
>4. 

#### python
>14\. 다음은 사용자로부터 입력받은 문자열에서 처음과 끝의 3글자를 추출한 후 합쳐서 출력하는 파이썬 코드에서 ㄱ에 들어갈 내용은?
```python
string = input("7문자 이상 문자열을 입력하시오:")
m = ㄱ
print(m)

입력값: Hello World
최종 출력: Helrld
```
>1. string[1:3]+string[-3:]
>2. string[:3]+string[-3:-1]
>3. string[0:3]+string[-3:] V
>4. string[0:]+string[:-1]

`string[0:3]+string[-3:]`
=`string[:3]+string[-3:]`

뒤의 슬라이싱에서 0은 쓸 수 없음

#### 효과적인 모듈 설계
>15\. 효과적인 모듈 설계를 위한 유의사항으로 거리가 먼 것은?
>1. 모듈간의 결합도를 약하게 하면 모듈 독립성이 향상된다.
>2. 복잡도와 중복성을 줄이고 일관성을 유지시킨다.
>3. 모듈의 기능은 예측이 가능해야 하며 지나치게 제한적이어야 한다. V
>4. 유지보수가 용이해야 한다.

지나치게 제한적????

#### 프로세스 스케줄링
>16\. HRN 방식으로 스케줄링 할 경우. 입력된 작업이 다음과 같을 때 처리되는 작업 순서로 옳은 것은?
>1. A -> B -> C -> D
>2. A -> C -> B -> D
>3. D -> B -> C -> A V
>4. D -> A -> B -> C
|작업|대기시간|실행시간|
|---|---|---|
|A|5|20|
|B|40|20|
|C|15|45|
|D|20|2|

Highest Response-ratio Next
A: (5+20)/20 = 1.25
B: (40+20)/20 = 3
C: (15+45)/45 = 1.333...
D: (20+2)/2 = 11 

#### 운영체제
>17\. 운영체제에 대한 설명으로 거리가 먼 것은?
>1. 다중 사용자와 다중 응용프로그램 환경 하에서 자원의 현재 상태를 파악하고 자원 분배를 위한 스케줄링을 담당한다.
>2. CPU, 메모리공간, 기억장치, 입출력장치 등의 자원을 관리한다.
>3. 운용체제의 종류로는 매크로 프로세서, 어셈블러, 컴파일러 등이 있다. V
>4. 입출력 장치와 사용자 프로그램을 제어한다. 

운영체제 종류: Window, Linux, Android 등등

#### 메모리 관리
>18\. 메모리 관리 기법 중 `Worst fit` 방법을 사용할 경우 10K 크기의 프로그램 실행을 위해서는 어느 부분에 할당되는가?
>1. No.2
>2. No.3
>3. No.4
>4. No.5 V
|영역번호|메모리크기|사용여부|
|No.1|8k|FREE|
|No.2|12k|FREE|
|No.3|10k|IN USE|
|No.4|20k|IN USE|
|No.5|16k|FREE|

메모리 관리 기법
- First fit 기법: 들어갈 수 있는 가장 처음 영역
- Worst fit 기법: 들어갈 수 있는 가장 큰 영역
- Best fit 기법: 들어갈 수 있는 가장 작은 영역

단편화
- 내부단편화: 한 영역에 들어갔을 때 영역에 남은 공간
- 외부단편화: 한 영역보다 프로그램이 클 때 할당되지 못한 부분

사용중인 메모리 공간은 사용할 수 없다. 

#### 결합도
>19\. 어떤 모듈이 `다른 모듈의 내부 논리 조직을 제어`하기 위한 목적으로 제어신호를 이용하여 통신하는 경우이며, 하위 모듈에서 상위 모듈로 제어신호가 이동하여 상위 모듈에게 처리 명령을 부여하는 권리 전도현상이 발생하게 되는 `결합도`는?
>1. data coupling
>2. stamp coupling
>3. control coupling V
>4. common coupling

#### bash
>20\. 다음 중 bash 쉘 스크립트에서 사용할 수 있는 제어문이 아닌 것은?
>1. if
>2. for
>3. repeat_do V
>4. while

repeat_do가 대체 뭔데